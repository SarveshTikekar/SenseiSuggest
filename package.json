{
  "name": "senseisuggest-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && uvicorn core.main:app --reload --port 8001",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "npm install --prefix frontend && pip install -r requirements.txt",
    "dev": "npm run dev:frontend"
  },
  "description": "Unified workspace for SenseiSuggest Frontend (Vite/React) and Backend (FastAPI)"
}
